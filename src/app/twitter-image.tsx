/**
 * The Twitter/X card is the same drawing as the Open Graph card. Re-exported
 * rather than duplicated so the two can never drift apart.
 */
export { alt, size, contentType, default } from "./opengraph-image";
